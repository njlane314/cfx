#include <bits/stdc++.h>

using namespace std;

using ll = long long;

void solve(istream& in = cin, ostream& out = cout) {
    (void)in;
    (void)out;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
    return 0;
}
